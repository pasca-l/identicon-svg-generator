version: "3"

services:
  app:
    build:
      context: .
      dockerfile: "Dockerfile"
    platform: linux/amd64
    volumes:
      - type: bind
        source: ./src/
        target: /home/local/src/
    stdin_open: true
    tty: true
